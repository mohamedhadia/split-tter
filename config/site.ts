export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "Split TTer",
  description: "Split TTer app built with nextjs and tailwindcss",
  mainNav: [
    {
      title: "Home",
      href: "/",
    },
  ],
}
